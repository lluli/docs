# factverifai environment variables (copy this file to .env)

# exa api key (required)
EXA_API_KEY=""

# ollama set to true for nvidia only gpu acceleration, false for cpu-only
USE_NVIDIA_GPU=false

# ollama connection
OLLAMA_BASE_URL=http://localhost:11434

# model config (change to switch models globally)
LOCAL_LLM_MODEL=gemma3:1b
DEFAULT_EXA_RESULTS=5
DEFAULT_MAX_WORKERS=4
